# PaperLoop 0.3.0 Windows 安装版

适用于 Windows 10 / 11 64 位电脑。安装包包含桌面运行环境、PDF 解析、OCR 模型和阅读界面，无需安装 Python、Node、Codex 或配置命令行。

## 安装和首次使用

1. 双击 `PaperLoop-Setup-0.3.0-win-x64.exe`，按提示安装。默认安装到当前用户的应用目录，也可以选择其他路径。
2. 从桌面或开始菜单打开 PaperLoop。首次打开会出现“模型连接”。
3. 填入自己的 DeepSeek API 密钥，点击“测试并保存”。地址和模型已填好。密钥由 [DeepSeek 官方平台](https://platform.deepseek.com/) 提供，调用费用由自己的账号承担。
4. 拖入英文 PDF，开始解析与中文翻译。没有密钥时，也可以先读原文、选择图表、添加批注。
5. 阅读时划选文字即可解释或提问；“研究任务”可以输入目标，让模型逐步收集论文证据并形成报告。

连接测试会发出一次很短的模型请求。保存过密钥后，设置只显示“已保存”，不会回显完整密钥。换密钥时重新填写；勾选“清除已保存的密钥”可以删除。高级设置支持其他兼容 Chat Completions 的服务，以及本机不需要密钥的服务。

## 本机保存与模型服务

原 PDF、阅读记录、译文、笔记和配置保存在 `%LOCALAPPDATA%\PaperLoop\data`，与安装目录分开。API 密钥由 Windows 当前用户身份加密保存；请勿把整个数据目录当作共享安装包发送。

程序在本机运行，模型生成使用你选择的服务。DeepSeek API 需要网络，相关论文文本或图像会发给 DeepSeek；这不等于把 DeepSeek 模型下载到电脑离线运行。安装包不包含开发者的密钥、个人论文、阅读记录或账号登录凭据。

安装版关闭窗口时会停止本地服务；重新打开后可以继续翻译，研究任务会保留为暂停，手动继续。更换安装路径或覆盖升级不会删除资料；卸载保留上述数据目录。

## 当前范围

- 支持 PDF 解析、扫描页 OCR、中英对照、图表放大、划词问答、批注、比较、阅读报告及有步数限制的 Agent Loop。
- API 模式当前没有独立联网搜索工具，因此不会开放联网查证功能；模型回答不能当作实时联网结果。
- 图像解释需要支持图片的模型，默认文字与图像均使用 `deepseek-flash`。可在高级设置调整。
- 尚未使用真实 DeepSeek 密钥完成端到端实测；安装、解析、OCR、密钥保存及兼容接口调用使用隔离测试验证。
- 此版本未做商业代码签名，Windows 可能提示未知发布者；请确认文件来自项目维护者，并核对配套 SHA256 校验值。

## 安装或启动问题

- 如电脑缺少微软 WebView2，安装程序会自动调用微软引导程序下载所需组件，这一步需要联网。
- 端口冲突会在窗口中显示原因；安装版默认仅监听 `127.0.0.1:8766`。源码开发版使用 `8765`。
- 启动失败可查看 `%LOCALAPPDATA%\PaperLoop\data\desktop-backend.log`。分享日志前请检查是否含个人论文信息。
- 升级或卸载前先关闭 PaperLoop，等待后台退出，再运行安装包。
