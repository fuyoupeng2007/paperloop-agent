## 2026-09-23：GitHub、高清图表与批注补充

- 作者查证增加个人 GitHub、组织账号和项目仓库；严格区分类型。已核实归属必须包含 GitHub 目标页与其他核对来源；单一页面、同名候选或仅 URL 参数变化不能当作交叉证据。模型的来源解释仍需人工判断。
- FigureViewer 按缩放和设备像素密度重绘 PDF，使用独立渲染画布避免旧缩放任务覆盖新结果。限制为 20 MP / 8192 px 边长。原始嵌入位图的像素仍限制最终细节。
- 原文划词保存逐行选区，译文批注关联原段落；图表整图／局部／柱子／单元格支持批注。四色、原页标记、编辑、删除和持久化均已接入。
- 新增批注 PUT 接口保留锚点与几何位置；无效坐标、跨页锚点、未知颜色及空白内容在保存前拒绝。HTML/Markdown 报告包含摘录、颜色与选区截图。
- 真实 Edge 在 DPR 2 下测试：图窗 100% 画布 1683×642，200% 画布重绘为 3366×1285；快速反向缩放不会被旧任务覆盖；DPR 3、400% 时画布为 7238×2762（约 19.99 MP），保持在上限内。批注标记移至选区右上方，原页／图窗点击编辑复测通过。整图、框选、原文划词批注完成新增、编辑、刷新保留和删除，原有内容未改变。
- 本机 ChatGPT 真实作者查询完成，4 个联网活动、4 个来源：找到了 `echo840` 个人账号候选（交叉身份信息不足，标为待核实）和已由论文链接确认的 `Yuliang-Liu/MonkeyOCR` 项目仓库。结果已保存在当前论文作者卡；没有把项目仓库的拥有者当作第一作者。
- 全部 Python 回归 **86 项通过**；前端 TypeScript/Vite 构建通过；作者/GitHub/机构及比较 UI 的隔离 Edge 测试通过。
- 更新前 SQLite 备份：`work/reader-before-annotations-20260923.db`，完整性检查通过。只添加新批注字段，未重写原文、翻译或既有问答。

# 本机桌面版验证记录

## 2026-09-23 · 可控论文研究 Agent Loop

- 阅读页新增“研究任务”：用户给目标后，模型每轮从论文检索、段落阅读、图表观察、联网查证、提交结论中选择动作；工具返回实际结果后再决定下一步。结论引用必须是已读取证据的编号，PDF 摘录还会与当前论文逐字核对；提交后由第二次模型检查依据和目标缺口。模型审查无法代替人工判读。
- 单任务默认 8 步，可设 3–16 步；每步最多配 3 次模型请求，并受每篇已有总调用上限约束。重复动作、无效引用、缺少结论依据会停下或转入补查。暂停／取消在当前请求完成并保存证据后生效；重启只恢复进度，不自动继续消耗额度。研究记录可导出 HTML／Markdown。
- 默认 ChatGPT 登录支持实际网页查询。通用 Chat Completions 连接可执行论文内研究，只有配置视觉模型才开放图表观察；当前没有为它提供联网搜索适配。DeepSeek 尚未用真实密钥验收。
- 后端全部 **117 项测试通过**（旧功能 86 项，研究循环 19 项，HTTP 与工具 12 项）；前端 TypeScript／Vite 构建通过；隔离浏览器覆盖研究入口、布局、暂停、续跑、引用回跳、来源与导出。旧论文 148 个内容块的部署前备份为 `../../work/reader-before-agent-loop-20260923.db`。
- 更新后发现 WindowsApps 内的 CLI 路径可解析但拒绝子进程启动；已改用同一官方 Codex 安装在 LocalAppData 的可运行 CLI，健康检查显示既有 ChatGPT 登录已连接。真实研究运行记录另行核对。

## 2026-09-23 · 第二、三批：局部理解与研究工作区

- 新增独立的 `object_details`、`reading_anchors`、`comparisons` 数据表。升级前在线备份：`../../work/reader-before-phase23-20260923.db`，部署前再备份 `../../work/reader-before-phase23-deploy-20260923.db`。部署后核对 148 个旧内容块、7 条旧问答及旧笔记完全保留；用户部署前阅读页为第 1 页，检查后仍为第 1 页，SQLite integrity_check 为 ok。
- 图表放大层支持本机 OCR 文字框点选、Shift 文字范围、框选、柱子候选、Shift 双柱、单元格、整行整列，以及文字/柱子人工修正。视觉问答同时发送局部与父图，以便核对图例、坐标轴和表头。各局部的对话与笔记分别保存，重新打开可定位。
- MonkeyOCR 上传 v1：图 1 得到 69 个文字框、43 根柱子。图例、类别、图上标注值和单位经本篇原图核对，并以精确文件指纹限定，**不是通用自动图表语义恢复**。`Overall = 84.5` 与 `Infer Speed = 0.84 Pages/s` 的单位和轴独立。通用图表只给彩色柱形候选，未核实数据留空。
- 表 1：保留原有 10×9 校正内容，90 个数据格与 11 个表头单独定位；保留 Supporting Tasks / Language 合并表头，分组空隙不算数据行，空值不填零。其他表格只有网格位置和既有结构匹配时开放单元格。可靠表格支持 CSV 导出。
- 阅读浮条增加“联网查”；背景页提供作者、机构深查及有来源的关系、相关/后续工作。失败保留上次结果，重启会标明被中断的查询。来源为本次搜索结果的模型整理，程序核查真实 web 事件、链接格式与关系来源归属，不独立抓取所有网页重证每项事实。
- 书架新增 2–5 篇比较工作区、九类可选维度、逐格原文证据、回到对应段落、独立连续追问与历史、Markdown 导出。对照单元格和追问引用须匹配实际传入的原文；匹配失败不展示无证据结论。当前真实书架只有一篇，跨篇流程通过隔离样例与模拟模型测试，尚未做两篇真实论文的比较质量评测。
- HTML 阅读报告包含背景、来源、笔记、问答和局部 PDF 截图，可独立打开或打印；Markdown 报告保留来源及页码/坐标。图片去重，最多 24 张、约 20 MB；超限保留文本定位。
- 75 项 Python 测试通过；TypeScript/Vite 构建通过。隔离 Edge 研究界面测试通过，覆盖选择限制、证据列映射、引用回跳、追问失败重试、重开恢复、作者关系与来源安全。真实 PDF 的隔离浏览器检查通过，覆盖柱/词框/表格/追问/笔记/CSV/含图报告；这些回归使用本机假模型。
- **真实 ChatGPT 验证**：选中推理速度柱，正确回答 0.84 的单位为 Pages/s、使用右轴，并与左轴 Overall 84.5 区分。该条有用问答保存到本篇，旧问答未变化。第一作者 Zhang Li 深查实际完成，保存 4 个来源、5 条有来源的关系和 5 条 web 活动。结果仍可由读者点击来源核对。
- 原生 WebView2 smoke-test 返回 `ok=true`、`backendHealthy=true`，截图与记录位于 `../../work/phase23-desktop-smoke.*`；本机 `8765` 服务加载新路由。局部功能仍以普通数字 PDF 和本篇样例为主要验证范围，任意复杂/扫描/旋转图表的通用性属于后续批次。

可重复检查：`tests/test_reading_details.py`、`tests/test_study.py`、`tests/study_browser.mjs`、`../../work/check_phase23_details.mjs`。真实请求结果记录：`../../work/phase23-live-visual.json`。

## 2026-09-23 · 阅读操作第一批

- 从 PDF 图像绘制位置和图注增量生成只读图表对象。MonkeyOCR 实际得到 6 图、3 表；图 1 本体坐标为 `[0.15505, 0.33706, 0.84274, 0.53989]`。第 5 页表 1 继续引用原有人工核对块，不重新解析或覆盖。
- 图/表单击选中、双击放大，放大层支持滚轮缩放和拖动；右侧助手仍可提问。原 PDF 和中文译文的划词浮条提供解释、翻译、提问、笔记；图表目录和连续阅读已加入。
- 图、手动框选区域、同段不同文字选区使用不同对话标识。视觉回答保存选区坐标，点击历史中的“图像依据”能返回原区域。
- Python 完整测试 45 项通过；TypeScript 和 Vite 生产构建通过；原有浏览器回归通过。真实 MonkeyOCR PDF 的 Edge 操作测试覆盖图 1 本体点选、放大、滚轮、右栏并用、划词、连续阅读。隔离数据上的本机假模型验证了同图连续追问、两处划词隔离、手动框选及重开后恢复；这些测试未调用用户的 ChatGPT 额度。
- 正式 8765 服务的只读浏览器验收通过。测试将跳页保存请求拦截在浏览器内，保留用户原来的第 4 页阅读进度。升级前数据库在线备份在 `../../work/reader-before-reading-operations-20260923.db`；核对确认原有 148 个内容块、旧笔记和 7 条旧问答仍保持原值。
- 当前可放大并围绕图表区域问答。图内位图文字划选、自动辨认单根柱子的指标和数值，以及表格单元格级提问属于下一批。

日期：2026-09-22。环境：Windows、Python 3.14.3、Node.js 24.19.0、.NET 10、Microsoft Edge WebView2。

## 真实使用已跑通

- 官方 Codex CLI 检测为 ChatGPT 已登录，使用保存的官方登录完成真实翻译，不复制凭据。
- 桌面 EXE 从任意工作目录可启动；已验证独立窗口渲染、自动启动后端、复用已运行服务，以及关窗后后台继续运行。
- 用户导入的《MonkeyOCR: Document Parsing with a Structure-Recognition-Relation Triplet Paradigm》14 页已完成处理：90 个内容块完成翻译，58 个作者信息、参考文献与页边等内容保留原文，零失败块，状态 completed。
- 标题与摘要人工抽查；真实段落问答解释了 SRR 三个部分并返回可定位原文；真实框选图表解释识别了第一页图例中的五个模型。
- 实际界面验证通过：无需显示高级 API 参数、中文标题、原 PDF 渲染、页边杂字移除、中文解释和引用高亮。测试未改变用户当前阅读页码。
- 真实论文的 Markdown 与双语 HTML 已导出，未包含未完成标记或已知 cid:33 乱码。

## 本篇解析修正

- 左侧竖排 arXiv 标识被旧解析器误拆成 J、5、]、V C 等正文，现隔离为完整页边注记。
- 两行标题合并为一个标题；作者与单位保留原文；纠正正文误判标题和图表间隙误判双栏的问题。
- 第 5 页表 1 的字体缺少 Unicode 映射，且自动提取漏掉一个数据集标签。已按原 PDF 渲染人工核对并重建这一张表，界面标注核对说明。这是本篇的人工修正，不表示自动解析器能准确重建任意复杂表格。
- 网址、专名与模型型号允许保留原文；正常英文正文仍必须返回中文，数字检查保持启用。

## 自动化检查

33 项 Python 测试通过：完整上传/解析/模拟模型流程，任务暂停与恢复，预算保护，官方 CLI 适配，批量部分失败，引用与笔记，版式回归，纯专名与数字检查，实际扫描页 OCR，以及联网记录、来源校验和段落上下文隔离。

TypeScript + Vite 生产构建通过。桌面 .NET 构建通过。修正数据库连接释放后，集成测试再次通过，未再出现未释放数据库连接警告。

## 本次优化的真实验证

- `web_search="live"` 已通过现有 ChatGPT 登录执行；MonkeyOCR 背景报告实际运行约 111 秒后完成，保存 4 个主要来源、上传 v1 的 10 位作者及研究意义。
- 报告区分上传版本、arXiv 当前版本及作者发布的期刊接收公告，不把 arXiv 或作者公告等同于正式出版记录。
- 请求必须有实际 CLI web_search 事件，空联网记录不会被当成成功报告；程序校验来源链接格式与引用编号，未逐条自动证明网页内容支持每个陈述。报告和来源仍需读者核对。
- 真实段落问答两轮完成：“SRR 三个部分各做什么？”后追问“第三部分为什么不能省略？”，两次均绑定相同段落并返回可核对的原文。阅读页码保持不变。
- Playwright 模拟检查通过：解释直接发送、连续追问、段落/章节/页面引用跨页后保留对话锚点、按范围查看历史、失败重试，以及背景异步刷新时保留旧报告。共 8 次模拟问答、1 次模拟研究。
- 对已部署服务的只读界面检查通过：10 位作者、4 个来源链接和2轮真实段落历史可见，无浏览器脚本错误；全部写请求在测试浏览器中被拦截。
- 原创深绿书页回环图标包含九档 ICO，已嵌入正式桌面 EXE；原生 WebView2 窗口启动截图验证通过。已更新用户桌面 PaperLoop 快捷方式并正常重新打开应用。
- MonkeyOCRv2 Windows CPU 路线完成资料与硬件评估，见 MONKEYOCR.md；当前没有下载其权重、安装其依赖或将其接入识别流程，不将评估结果当成运行测试。

## 验证边界

已跑通本机与这一篇真实论文，不代表完成了多学科大规模译文质量评测。复杂表格、公式和图内文字仍应对照原 PDF；第一版不重绘原版式的翻译 PDF。程序和文档保存在本机，ChatGPT 翻译与解释需要联网，并受账号的 Codex 额度限制。
